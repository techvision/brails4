$('#flash').html('#{escape_javascript(render partial: 'layouts/messages')}')
- if @attempt.present?
  $('#questions').html('#{escape_javascript(render partial: 'contents/question', locals: {attempt: @attempt, question: @attempt.question})}')

